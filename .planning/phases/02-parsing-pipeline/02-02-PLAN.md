---
phase: 02-parsing-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/assets/types.ts
  - src/assets/buildManifest.ts
  - src/assets/copy.ts
  - src/assets/buildManifest.test.ts
autonomous: true
requirements:
  - ASSET-01
  - ASSET-02
  - ASSET-03
  - ASSET-04

must_haves:
  truths:
    - "images/ directory is copied to .shipstudio/assets/images/"
    - "icons/ directory (including lucide/ subdirectory) is copied to .shipstudio/assets/icons/"
    - "Google Font URLs are included in the asset manifest"
    - "Asset manifest contains file counts and project-relative paths"
  artifacts:
    - path: "src/assets/buildManifest.ts"
      provides: "Enumerate images and icons from ZIP entries list"
      exports: ["buildAssetManifest"]
    - path: "src/assets/copy.ts"
      provides: "Copy images/ and icons/ to .shipstudio/assets/ via shell.exec"
      exports: ["copyAssets"]
    - path: "src/assets/buildManifest.test.ts"
      provides: "Unit tests for manifest building"
  key_links:
    - from: "src/assets/copy.ts"
      to: "shell.exec"
      via: "cp -r for directory copying"
      pattern: "shell\\.exec.*cp"
    - from: "src/assets/buildManifest.ts"
      to: "src/assets/types.ts"
      via: "imports AssetManifest, AssetEntry"
      pattern: "import.*AssetManifest.*from.*types"
---

<objective>
Implement asset enumeration and copying — images and icons from the WeWeb export to .shipstudio/assets/.

Purpose: Assets must be available at project-relative paths so the brief can reference them. This is independent of design token and page analysis work.

Output: Asset copy module and manifest builder with tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-parsing-pipeline/02-RESEARCH.md
@src/types.ts
@src/zip/types.ts
@src/zip/extract.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build asset manifest from ZIP entries list</name>
  <files>src/assets/types.ts, src/assets/buildManifest.ts, src/assets/buildManifest.test.ts</files>
  <read_first>
    - src/zip/types.ts (ZipManifest.entries — the input list)
    - src/zip/discover.ts (parseUnzipManifest — shows entries format)
    - .planning/phases/02-parsing-pipeline/02-RESEARCH.md (lines 762-779 — asset module patterns)
  </read_first>
  <behavior>
    - buildAssetManifest(entries, googleFontUrls): given ZIP entry strings and font URLs, returns AssetManifest
    - entries like "images/photo.jpg" become AssetEntry { filename: "photo.jpg", projectRelativePath: ".shipstudio/assets/images/photo.jpg" }
    - entries like "icons/lucide/arrow.svg" become AssetEntry { filename: "arrow.svg", projectRelativePath: ".shipstudio/assets/icons/lucide/arrow.svg" }
    - Directory entries (ending with "/") are excluded from file lists
    - entries in "assets/" directory are SKIPPED (compiled JS/CSS bundles)
    - totalCopied = images.length + icons.length
    - googleFonts passed through from parameter
    - Empty entries returns empty manifest with totalCopied: 0
  </behavior>
  <action>
**src/assets/types.ts** — If not created by Plan 01 yet, create with:
- `export interface AssetEntry { filename: string; projectRelativePath: string; }`
- `export interface AssetManifest { images: AssetEntry[]; icons: AssetEntry[]; googleFonts: string[]; totalCopied: number; }`

**src/assets/buildManifest.ts** — export function `buildAssetManifest(entries: string[], googleFontUrls: string[]): AssetManifest`:
1. Filter entries starting with `images/` that do NOT end with `/` — these are image files
2. For each image entry: `{ filename: entry.split('/').pop()!, projectRelativePath: '.shipstudio/assets/' + entry }`
3. Filter entries starting with `icons/` that do NOT end with `/` — these are icon files (includes `icons/lucide/...`)
4. For each icon entry: `{ filename: entry.split('/').pop()!, projectRelativePath: '.shipstudio/assets/' + entry }`
5. `totalCopied = images.length + icons.length`
6. Return `{ images, icons, googleFonts: googleFontUrls, totalCopied }`

**src/assets/buildManifest.test.ts** — vitest tests:
- Test with entries `["images/", "images/photo.jpg", "images/logo.png", "icons/", "icons/close.svg", "icons/lucide/", "icons/lucide/arrow.svg"]` + googleFonts `["https://fonts.googleapis.com/css2?family=Work+Sans"]`
- Expect images.length === 2, icons.length === 2, totalCopied === 4, googleFonts.length === 1
- Expect images[0].projectRelativePath === ".shipstudio/assets/images/photo.jpg"
- Expect icons[1].projectRelativePath === ".shipstudio/assets/icons/lucide/arrow.svg"
- Test empty entries returns empty manifest
- Test entries with "assets/bundle.js" are NOT included
  </action>
  <verify>
    <automated>npx vitest run src/assets/buildManifest.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/assets/buildManifest.ts exports `buildAssetManifest`
    - Function filters `images/` and `icons/` entries, skips directory entries (ending with "/")
    - Function does NOT include `assets/` directory entries
    - projectRelativePath starts with `.shipstudio/assets/`
    - totalCopied equals images.length + icons.length
    - All tests pass
  </acceptance_criteria>
  <done>Asset manifest builder correctly enumerates images and icons from ZIP entries with project-relative paths.</done>
</task>

<task type="auto">
  <name>Task 2: Implement asset copy via shell.exec</name>
  <files>src/assets/copy.ts</files>
  <read_first>
    - src/types.ts (Shell interface — exec signature)
    - src/zip/extract.ts (extractAndVerify — shows shell.exec patterns for mkdir -p and cp)
    - src/assets/types.ts (AssetManifest interface)
  </read_first>
  <action>
**src/assets/copy.ts** — export async function `copyAssets(shell: Shell, extractDir: string, projectPath: string): Promise<void>`:

1. Create destination directories:
   ```
   await shell.exec('mkdir', ['-p', `${projectPath}/.shipstudio/assets/images`]);
   await shell.exec('mkdir', ['-p', `${projectPath}/.shipstudio/assets/icons`]);
   ```

2. Copy images/ if it exists (graceful skip if missing):
   ```
   const imgCheck = await shell.exec('bash', ['-c', `test -d '${extractDir}/images' && echo exists || echo none`]);
   if (imgCheck.stdout.trim() === 'exists') {
     await shell.exec('bash', ['-c', `cp -r '${extractDir}/images/'* '${projectPath}/.shipstudio/assets/images/' 2>/dev/null || true`]);
   }
   ```

3. Copy icons/ if it exists (graceful skip if missing — includes lucide/ subdirectory):
   ```
   const iconCheck = await shell.exec('bash', ['-c', `test -d '${extractDir}/icons' && echo exists || echo none`]);
   if (iconCheck.stdout.trim() === 'exists') {
     await shell.exec('bash', ['-c', `cp -r '${extractDir}/icons/'* '${projectPath}/.shipstudio/assets/icons/' 2>/dev/null || true`]);
   }
   ```

The function does NOT throw if directories are missing — WeWeb exports may not always have images or icons. The `2>/dev/null || true` suppresses errors from empty directories.

Mirror the pattern from webflow-to-code `assets/copy.ts` — check existence first, then cp -r, graceful on missing.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/assets/copy.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/assets/copy.ts exports `copyAssets` async function
    - Function accepts `(shell: Shell, extractDir: string, projectPath: string)`
    - Function creates `.shipstudio/assets/images` and `.shipstudio/assets/icons` via `mkdir -p`
    - Function checks directory existence with `test -d` before copying
    - Function uses `cp -r` for recursive copy (handles icons/lucide/ subdirectory)
    - Function does NOT throw on missing source directories
    - File compiles with `npx tsc --noEmit`
  </acceptance_criteria>
  <done>Asset copy module gracefully copies images/ and icons/ from extracted WeWeb export to .shipstudio/assets/, handling missing directories without errors.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for all new files
- `npx vitest run src/assets/` passes all tests
- copyAssets handles missing directories gracefully (no throw)
- buildManifest correctly filters entries by prefix and excludes directories
</verification>

<success_criteria>
- Asset manifest lists images and icons with correct project-relative paths
- Asset copy creates destination dirs and copies recursively
- Missing source dirs do not cause errors
- ASSET-01 through ASSET-04 addressed
</success_criteria>

<output>
After completion, create `.planning/phases/02-parsing-pipeline/02-02-SUMMARY.md`
</output>
